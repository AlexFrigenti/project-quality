# Tareas: Coherencia de `propertyNames` en `quality-metrics`

- [x] Crear artefactos T1 (`spec.md`, `plan.md`, `tasks.md`).
- [x] Declarar `propertyNames` en `properties.metrics` y `$defs.metricValue` dentro de `schemas/quality-metrics.schema.json`.
- [x] Ampliar `scripts/test-quality-metrics.mjs` con aserciones sobre el schema y batería de nombres válidos/inválidos en raíz y anidados.
- [x] Ejecutar las 7 suites de validación y comprobar que finalizan con EXIT 0.
- [x] Ejecutar `git diff --check` y realizar autorrevisión adversarial.
