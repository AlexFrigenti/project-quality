# Tareas: Unificación de etiqueta de estado de proceso `pending` en `history.html`

- [x] Crear artefactos T1 (`spec.md`, `plan.md`, `tasks.md`).
- [x] Ampliar `scripts/test-history-rendering.mjs` con regresiones para `processLabels.pending` y renderizado de proceso `pending`.
- [x] Actualizar `pending: "En curso"` en `processLabels` dentro de `dashboard/history.html`.
- [x] Ejecutar las siete suites de validación y verificar que todas finalizan con EXIT 0.
- [x] Ejecutar `git diff --check` y realizar autorrevisión adversarial.
