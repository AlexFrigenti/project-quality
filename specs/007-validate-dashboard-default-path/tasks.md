# Tareas: Corrección de la ruta por defecto en `validate-dashboard.mjs`

- [x] Crear artefactos T1 (`spec.md`, `plan.md`, `tasks.md`).
- [x] Modificar el default CLI en `scripts/validate-dashboard.mjs` a `site/data.json`.
- [x] Añadir pruebas de regresión CLI en `scripts/test-dashboard.mjs` para casos sin argumentos, con ruta explícita y con default ausente.
- [x] Ejecutar las siete suites de validación y comprobar que pasan en verde.
- [x] Ejecutar `git diff --check` y realizar autorrevisión adversarial.
